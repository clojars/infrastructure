---
- hosts: "{{ host | default('127.0.0.1') }}" 
  remote_user: ec2-user
  become: true
  become_user: root
  vars_files:
    - /tmp/clojars_vars.yml # generated by setup_ansible_vars.sh

  roles:
    - system
    - correttojdk
    - leiningen
    - clojars
  
