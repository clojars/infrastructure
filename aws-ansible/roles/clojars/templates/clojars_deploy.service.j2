# {{ ansible_managed }}
[Unit]
Description=Deploy Clojars from S3 on boot
After=network.target

[Service]
Type=oneshot
RemainAfterExit=true

User=clojars
WorkingDirectory=/home/clojars

ExecStart=/home/clojars/bin/deploy-clojars --disable-restart

[Install]
WantedBy=multi-user.target